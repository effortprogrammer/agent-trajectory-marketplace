import { withTrailingNewline } from "./template-utils"

export { renderPythonBootstrap } from "./workspace-bootstrap-template"

export const renderInitReadme = (runtime: string) =>
  withTrailingNewline([
    "# Agent Trajectory Prototype",
    "",
    "This workspace is a local Phase 0 collector prototype generated by `trajectory-marketplace trajectory init`.",
    "It is intentionally designed for ephemeral manual QA workspaces such as `.tmp/trajectory-e2e`.",
    "",
    "Generated runtime:",
    `- ${runtime}`,
    "",
    "Quickstart:",
    "1. Inspect the pattern files under `patterns/`.",
    "2. Run the demo through the CLI wrapper:",
    `   bun src/cli/index.ts trajectory demo ${runtime} --workspace . --export ./artifacts/trace.atf.json`,
    "3. Inspect the exported ATF JSON and SQLite buffer under `artifacts/`.",
  ])

export const renderPatternYaml = (runtime: string, functionName: string) =>
  withTrailingNewline([`runtime: ${runtime}`, "module: demo_target", `function: ${functionName}`])

export const renderPythonPackageInit = () =>
  withTrailingNewline([
    '"""Agent trajectory collector prototype package."""',
    "",
    "from .bootstrap import export_trace, install_hook",
    "",
    '__all__ = ["export_trace", "install_hook"]',
  ])

export const renderPythonCore = () =>
  withTrailingNewline([
    '"""SQLite-backed trace store for the agent trajectory prototype."""',
    "from __future__ import annotations",
    "",
    "import json",
    "import sqlite3",
    "from dataclasses import dataclass",
    "from pathlib import Path",
    "",
    "",
    "@dataclass(frozen=True)",
    "class TrajectoryEvent:",
    '    """One recorded event in the local trajectory buffer."""',
    "",
    "    kind: str",
    "    name: str",
    "    detail: str",
    "",
    "",
    "class SqliteTraceStore:",
    '    """Append-only event buffer that can export an ATF-style JSON artifact."""',
    "",
    "    def __init__(self, db_path: Path) -> None:",
    "        self._db_path = db_path",
    "        self._db_path.parent.mkdir(parents=True, exist_ok=True)",
    "        self._initialize()",
    "",
    "    def _initialize(self) -> None:",
    "        with sqlite3.connect(self._db_path) as connection:",
    "            connection.execute(",
    '                "CREATE TABLE IF NOT EXISTS trajectory_events (kind TEXT NOT NULL, name TEXT NOT NULL, detail TEXT NOT NULL)"',
    "            )",
    "",
    "    def append(self, event: TrajectoryEvent) -> None:",
    '        """Persist one event in insertion order."""',
    "",
    "        with sqlite3.connect(self._db_path) as connection:",
    "            connection.execute(",
    '                "INSERT INTO trajectory_events (kind, name, detail) VALUES (?, ?, ?)"',
    "                ,",
    "                (event.kind, event.name, event.detail),",
    "            )",
    "",
    "    def export_atf(self, export_path: Path, runtime: str, status: str) -> dict[str, object]:",
    '        """Write an ATF-style JSON document and return its parsed payload."""',
    "",
    "        with sqlite3.connect(self._db_path) as connection:",
    "            rows = connection.execute(",
    '                "SELECT kind, name, detail FROM trajectory_events ORDER BY rowid ASC"',
    "            ).fetchall()",
    "",
    "        events = [",
    '            {"kind": kind, "name": name, "detail": detail}',
    "            for kind, name, detail in rows",
    "        ]",
    "        payload = {",
    '            "runtime": runtime,',
    '            "status": status,',
    '            "eventCount": len(events),',
    '            "events": events,',
    "        }",
    "        export_path.parent.mkdir(parents=True, exist_ok=True)",
    '        export_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")',
    "        return payload",
  ])

export const renderPythonFixture = (runtime: string) =>
  withTrailingNewline([
    '"""Tiny runtime fixture for the trajectory demo."""',
    "from __future__ import annotations",
    "",
    "from trajectory_collector.bootstrap import emit_event",
    "",
    "",
    "def run_demo_task() -> str:",
    '    """Simulate one runtime loop with LLM, tool, and verification events."""',
    "",
    `    emit_event("llm_call", "${runtime}.plan", "draft a trajectory collection plan")`,
    '    emit_event("tool_call", "read_file", "fixtures/demo_target.py")',
    `    emit_event("verification", "trajectory.demo", "${runtime} collector completed with secret-demo-token")`,
    '    return "demo_target_completed"',
  ])
