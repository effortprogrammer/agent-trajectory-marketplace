import { withTrailingNewline } from "./template-utils"

export const renderPythonDemoRunner = () =>
  withTrailingNewline([
    '"""Trusted runner for the generated trajectory prototype workspace."""',
    "from __future__ import annotations",
    "",
    "import ast",
    "import importlib",
    "import importlib.abc",
    "import importlib.machinery",
    "import json",
    "import os",
    "import sqlite3",
    "import sys",
    "import tempfile",
    "from dataclasses import dataclass",
    "from pathlib import Path",
    "from typing import Optional",
    "",
    "_STORE = None",
    '_STATUS = "not_configured"',
    "_ACTIVE = False",
    "",
    "@dataclass(frozen=True)",
    "class TrajectoryEvent:",
    "    kind: str",
    "    name: str",
    "    detail: str",
    "",
    "class SqliteTraceStore:",
    "    def __init__(self, db_path: Path) -> None:",
    "        self._db_path = db_path",
    "        self._db_path.parent.mkdir(parents=True, exist_ok=True)",
    "        self._initialize()",
    "",
    "    def _initialize(self) -> None:",
    "        with sqlite3.connect(self._db_path) as connection:",
    "            connection.execute(",
    '                "CREATE TABLE IF NOT EXISTS trajectory_events (kind TEXT NOT NULL, name TEXT NOT NULL, detail TEXT NOT NULL)"',
    "            )",
    "",
    "    def append(self, event: TrajectoryEvent) -> None:",
    "        with sqlite3.connect(self._db_path) as connection:",
    "            connection.execute(",
    '                "INSERT INTO trajectory_events (kind, name, detail) VALUES (?, ?, ?)"',
    "                ,",
    "                (event.kind, event.name, event.detail),",
    "            )",
    "",
    "    def export_atf(self, export_path: Path, runtime: str, status: str) -> dict[str, object]:",
    "        with sqlite3.connect(self._db_path) as connection:",
    "            rows = connection.execute(",
    '                "SELECT kind, name, detail FROM trajectory_events ORDER BY rowid ASC"',
    "            ).fetchall()",
    "        safe_events = [",
    '            {"kind": kind, "name": name, "detail": _redact(detail)}',
    "            for kind, name, detail in rows",
    "        ]",
    '        payload = {"runtime": runtime, "status": status, "eventCount": len(safe_events), "events": safe_events}',
    "        export_path.parent.mkdir(parents=True, exist_ok=True)",
    '        export_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")',
    "        return payload",
    "",
    "def _redact(detail: str) -> str:",
    '    markers = ("token", "secret", "authorization", "bearer", "api_key")',
    "    lowered = detail.lower()",
    '    return "[redacted]" if any(marker in lowered for marker in markers) else detail',
    "",
    "@dataclass(frozen=True)",
    "class PatternSpec:",
    "    module_name: str",
    "    function_name: str",
    "",
    "def _set_status(value: str) -> None:",
    "    global _STATUS",
    "    _STATUS = value",
    "",
    "def _set_active(value: bool) -> None:",
    "    global _ACTIVE",
    "    _ACTIVE = value",
    "",
    "def _parse_pattern(pattern_path: Path) -> PatternSpec:",
    "    raw_pairs = {}",
    '    for line in pattern_path.read_text(encoding="utf-8").splitlines():',
    "        stripped = line.strip()",
    '        if not stripped or stripped.startswith("#"):',
    "            continue",
    '        key, _, value = stripped.partition(":")',
    "        raw_pairs[key.strip()] = value.strip()",
    '    module_name = raw_pairs["module"]',
    '    function_name = raw_pairs["function"]',
    '    module_parts = module_name.split(".")',
    "    if not module_parts or any(not part.isidentifier() for part in module_parts):",
    '        raise RuntimeError(f"invalid_pattern_module: {module_name}")',
    "    if not function_name.isidentifier():",
    '        raise RuntimeError(f"invalid_pattern_function: {function_name}")',
    "    return PatternSpec(module_name=module_name, function_name=function_name)",
    "",
    'def emit_event(kind: str, name: str, detail: str = "") -> None:',
    "    if _STORE is None or not _ACTIVE:",
    "        return",
    "    _STORE.append(TrajectoryEvent(kind=kind, name=name, detail=_redact(detail)))",
    "",
    'sys.modules["trajectory_demo_support"] = sys.modules[__name__]',
    "",
    "def _write_fixture_module(fixture_dir: Path, module_name: str, fixture_source: str) -> None:",
    "    parts = module_name.split('.')",
    "    current_dir = fixture_dir",
    "    for package_name in parts[:-1]:",
    "        current_dir = current_dir / package_name",
    "        current_dir.mkdir(parents=True, exist_ok=True)",
    "        (current_dir / '__init__.py').write_text('', encoding='utf-8')",
    "    (current_dir / f'{parts[-1]}.py').write_text(fixture_source, encoding='utf-8')",
    "",
    "class _FunctionInstrumenter(ast.NodeTransformer):",
    "    def __init__(self, function_name: str) -> None:",
    "        self._function_name = function_name",
    "        self.instrumented = False",
    "",
    "    def visit_FunctionDef(self, node: ast.FunctionDef) -> ast.AST:",
    "        if node.name != self._function_name:",
    "            return self.generic_visit(node)",
    "        self.instrumented = True",
    "        before_call = ast.Expr(",
    '            value=ast.Call(func=ast.Name(id="_trajectory_before", ctx=ast.Load()), args=[ast.Constant(node.name)], keywords=[]),',
    "        )",
    "        after_call = ast.Expr(",
    '            value=ast.Call(func=ast.Name(id="_trajectory_after", ctx=ast.Load()), args=[ast.Constant(node.name)], keywords=[]),',
    "        )",
    "        node.body = [before_call, ast.Try(body=list(node.body), handlers=[], orelse=[], finalbody=[after_call])]",
    "        return self.generic_visit(node)",
    "",
    "",
    "class _TrajectoryLoader(importlib.abc.Loader):",
    "    def __init__(self, wrapped_loader: importlib.abc.Loader, pattern: PatternSpec) -> None:",
    "        self._wrapped_loader = wrapped_loader",
    "        self._pattern = pattern",
    "",
    "    def create_module(self, spec: importlib.machinery.ModuleSpec) -> Optional[object]:",
    '        create_module = getattr(self._wrapped_loader, "create_module", None)',
    "        if callable(create_module):",
    "            return create_module(spec)",
    "        return None",
    "",
    "    def exec_module(self, module: object) -> None:",
    "        if not isinstance(self._wrapped_loader, importlib.machinery.SourceFileLoader):",
    "            self._wrapped_loader.exec_module(module)",
    '            _set_status("skipped_pattern_mismatch")',
    "            return",
    "        source = self._wrapped_loader.get_source(module.__name__)",
    "        if source is None:",
    "            self._wrapped_loader.exec_module(module)",
    '            _set_status("skipped_pattern_mismatch")',
    "            return",
    "        tree = ast.parse(source, filename=self._wrapped_loader.path)",
    "        instrumenter = _FunctionInstrumenter(self._pattern.function_name)",
    "        transformed = instrumenter.visit(tree)",
    "        ast.fix_missing_locations(transformed)",
    "        module_dict = module.__dict__",
    '        module_dict["_trajectory_before"] = lambda function_name: emit_event("function_enter", function_name)',
    '        module_dict["_trajectory_after"] = lambda function_name: emit_event("function_exit", function_name)',
    '        compiled = compile(transformed, self._wrapped_loader.path, "exec")',
    "        if instrumenter.instrumented:",
    "            _set_active(True)",
    '            _set_status("instrumented")',
    '            emit_event("session_start", self._pattern.module_name, self._pattern.function_name)',
    "        else:",
    '            _set_status("skipped_pattern_mismatch")',
    "        exec(compiled, module_dict)",
    "",
    "",
    "class _TrajectoryFinder(importlib.abc.MetaPathFinder):",
    "    def __init__(self, pattern: PatternSpec) -> None:",
    "        self._pattern = pattern",
    "",
    "    def find_spec(self, fullname: str, path: object, target: Optional[object] = None) -> Optional[importlib.machinery.ModuleSpec]:",
    "        if fullname != self._pattern.module_name:",
    "            return None",
    "        spec = importlib.machinery.PathFinder.find_spec(fullname, path)",
    "        if spec is None or spec.loader is None:",
    "            return spec",
    "        spec.loader = _TrajectoryLoader(spec.loader, self._pattern)",
    "        return spec",
    "",
    "",
    "def install_hook(pattern_path: Path, sqlite_path: Path) -> PatternSpec:",
    "    global _STORE",
    "    _STORE = SqliteTraceStore(sqlite_path)",
    '    _set_status("armed")',
    "    _set_active(False)",
    "    pattern = _parse_pattern(pattern_path)",
    "    sys.meta_path.insert(0, _TrajectoryFinder(pattern))",
    "    return pattern",
    "",
    "",
    "def export_trace(export_path: Path, runtime: str) -> dict[str, object]:",
    "    if _STORE is None:",
    '        raise RuntimeError("collector store not configured")',
    "    return _STORE.export_atf(export_path=export_path, runtime=runtime, status=_STATUS)",
    "",
    "",
    "def _require_env(name: str) -> str:",
    '    """Read one required environment variable for the demo run."""',
    "",
    "    value = os.environ.get(name)",
    '    if value is None or value.strip() == "":',
    '        raise RuntimeError(f"missing required environment variable: {name}")',
    "    return value",
    "",
    "",
    "def main() -> int:",
    '    """Run the instrumented demo target and print a JSON summary."""',
    "",
    '    runtime = _require_env("TRAJECTORY_RUNTIME")',
    '    pattern_path = Path(_require_env("TRAJECTORY_PATTERN_PATH"))',
    '    export_path = Path(_require_env("TRAJECTORY_EXPORT_PATH"))',
    '    sqlite_path = Path(_require_env("TRAJECTORY_SQLITE_PATH"))',
    "    pattern = install_hook(pattern_path=pattern_path, sqlite_path=sqlite_path)",
    '    fixture_dir = Path(tempfile.mkdtemp(prefix="trajectory-marketplace-runner-"))',
    '    fixture_source = "\\n".join([',
    '        "from trajectory_demo_support import emit_event",',
    '        "",',
    '        "def run_demo_task() -> str:",',
    '        f"    emit_event(\\"llm_call\\", \\"{runtime}.plan\\", \\"draft a trajectory collection plan\\")",',
    '        "    emit_event(\\"tool_call\\", \\"read_file\\", \\"fixtures/demo_target.py\\")",',
    '        f"    emit_event(\\"verification\\", \\"trajectory.demo\\", \\"{runtime} collector completed with secret-demo-token\\")",',
    '        "    return \\"demo_target_completed\\"",',
    "    ])",
    "    _write_fixture_module(fixture_dir, pattern.module_name, fixture_source)",
    "    sys.path.insert(0, str(fixture_dir))",
    "    demo_target = importlib.import_module(pattern.module_name)",
    "    target_result = demo_target.run_demo_task()",
    "    export_payload = export_trace(export_path=export_path, runtime=runtime)",
    "    summary = {",
    '        "status": export_payload["status"],',
    '        "eventCount": export_payload["eventCount"],',
    '        "exportPath": str(export_path),',
    '        "sqlitePath": str(sqlite_path),',
    '        "targetResult": target_result,',
    "    }",
    "    print(json.dumps(summary))",
    "    return 0",
    "",
    "",
    'if __name__ == "__main__":',
    "    raise SystemExit(main())",
  ])
