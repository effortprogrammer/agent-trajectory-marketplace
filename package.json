{
  "name": "agent-trajectory-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "trajectory-marketplace": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dev": "bun src/cli/index.ts",
    "dev:trajectory": "zsh scripts/run-trajectory-dev.sh",
    "build": "bun build src/cli/index.ts --outdir dist --target bun",
    "start": "bun src/cli/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "commander": "^14.0.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "bun-types": "^1.3.11",
    "typescript": "^6.0.2"
  }
}
